<!DOCTYPE html>
<html lang="az">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PDF Analizatoru ilə Arayış Generatoru</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #1a73e8;
            --background-color: #f0f2f5;
            --form-background: #ffffff;
            --text-color: #333;
            --border-color: #d1d5db;
            --border-radius: 8px;
        }
        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--background-color);
            color: var(--text-color);
            margin: 0;
            padding: 2rem;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .container {
            width: 100%;
            max-width: 700px;
            background: var(--form-background);
            padding: 2.5rem;
            border-radius: var(--border-radius);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        h2 {
            color: var(--primary-color);
            margin-bottom: 1rem;
            font-weight: 600;
        }
        p {
            color: #666;
            margin-bottom: 2rem;
        }
        .upload-area {
            border: 2px dashed var(--primary-color);
            border-radius: var(--border-radius);
            padding: 2.5rem;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        .upload-area:hover {
            background-color: #f0f8ff;
        }
        .upload-area input[type="file"] {
            display: none;
        }
        .upload-area label {
            font-weight: 500;
            color: var(--primary-color);
        }
        button {
            width: 100%;
            background-color: var(--primary-color);
            color: white;
            padding: 0.85rem;
            border: none;
            border-radius: var(--border-radius);
            cursor: pointer;
            font-size: 1.1rem;
            font-weight: 500;
            margin-top: 2rem;
            transition: background-color 0.3s, transform 0.2s;
        }
        button:hover {
            background-color: #1765cc;
            transform: translateY(-2px);
        }
        .error {
            color: #d93025;
            margin-top: 1rem;
            font-weight: 500;
        }
        #file-list {
            margin-top: 1.5rem;
            text-align: left;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>PDF Faylları Analiz Et</h2>
        <p>Arayış tərtib etmək üçün bir və ya bir neçə PDF sənədi seçin.</p>
        
        <form method="post" enctype="multipart/form-data">
            {% csrf_token %}
            <div class="upload-area" onclick="document.getElementById('pdf_files').click();">
                <input type="file" name="pdf_files" id="pdf_files" multiple accept=".pdf" onchange="updateFileList()">
                <label for="pdf_files">Faylları seçmək üçün bura klikləyin</label>
            </div>
            <div id="file-list"></div>
            <button type="submit">Analiz Et və Arayış Yarat</button>
        </form>
        
        {% if error %}
            <p class="error">{{ error }}</p>
        {% endif %}
    </div>

    <script>
        // Seçilən faylların adlarını göstərmək üçün kiçik bir JavaScript kodu
        function updateFileList() {
            const input = document.getElementById('pdf_files');
            const fileList = document.getElementById('file-list');
            fileList.innerHTML = ''; // Köhnə siyahını təmizlə
            if (input.files.length > 0) {
                const list = document.createElement('ul');
                for (const file of input.files) {
                    const listItem = document.createElement('li');
                    listItem.textContent = file.name;
                    list.appendChild(listItem);
                }
                fileList.appendChild(list);
            }
        }
    </script>
</body>
</html>
